        PARSING:
-Mauvaise extention == ok
-Character invalide == ok
-Trop de player == ok
-Pas de player == ok
-Texture not given == ok
-Map not close == ok 
-Valeur RGB qui sont fausse == ok
-Acces aux path des textures == ok

> peut-etre ajouter un message adapte si la map est vide 
> peut-etre ajouter un message d'erreur adapte sil y a que la map et pas les textures + RGB

